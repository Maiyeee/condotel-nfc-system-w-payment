import {useMemo,useState} from 'react'
import {Plus,Trash2,Pencil,ShieldCheck} from 'lucide-react'
import SettingsSection from './SettingsSection'
const INITIAL=[{id:'usr-001',name:'Admin User',email:'admin@condotel.com',role:'Admin',status:'Active'},{id:'usr-002',name:'Front Desk Staff',email:'staff@condotel.com',role:'Staff',status:'Active'}]
export default function UserManagement(){const[users,setUsers]=useState(INITIAL),[q,setQ]=useState(''),[edit,setEdit]=useState(null);const filtered=useMemo(()=>users.filter(u=>`${u.name} ${u.email} ${u.role}`.toLowerCase().includes(q.toLowerCase())),[users,q]);const add=()=>{const id=`usr-${Date.now()}`;setUsers(u=>[...u,{id,name:'New User',email:'newuser@condotel.com',role:'Staff',status:'Active'}]);setEdit(id)};const upd=(id,k,v)=>setUsers(u=>u.map(x=>x.id===id?{...x,[k]:v}:x));return <SettingsSection title="Users & Access" description="Manage demo staff accounts and application roles."><div className="mb-4 flex gap-3"><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search users..." className="flex-1 rounded-lg border border-slate-300 px-3 py-2.5 text-sm"/><button onClick={add} className="inline-flex items-center gap-2 rounded-lg bg-navy-900 px-4 py-2 text-sm text-white"><Plus className="h-4 w-4"/>Add User</button></div><div className="overflow-x-auto rounded-lg border border-slate-200"><table className="min-w-full text-sm"><thead className="bg-slate-50 text-left"><tr><th className="px-4 py-3">User</th><th className="px-4 py-3">Role</th><th className="px-4 py-3">Status</th><th className="px-4 py-3">Actions</th></tr></thead><tbody className="divide-y divide-slate-100">{filtered.map(u=><tr key={u.id}><td className="px-4 py-4">{edit===u.id?<div className="space-y-2"><input value={u.name} onChange={e=>upd(u.id,'name',e.target.value)} className="w-full rounded border px-2 py-1"/><input value={u.email} onChange={e=>upd(u.id,'email',e.target.value)} className="w-full rounded border px-2 py-1"/></div>:<><div className="font-medium">{u.name}</div><div className="text-xs text-slate-500">{u.email}</div></>}</td><td className="px-4 py-4"><select value={u.role} onChange={e=>upd(u.id,'role',e.target.value)} className="rounded border px-2 py-1"><option>Admin</option><option>Staff</option></select></td><td className="px-4 py-4"><button onClick={()=>upd(u.id,'status',u.status==='Active'?'Inactive':'Active')} className="rounded-full bg-emerald-50 px-2.5 py-1 text-xs text-emerald-700">{u.status}</button></td><td className="px-4 py-4"><div className="flex gap-2"><button onClick={()=>setEdit(edit===u.id?null:u.id)} className="inline-flex items-center gap-1 rounded border px-2 py-1 text-xs"><Pencil className="h-3 w-3"/>{edit===u.id?'Done':'Edit'}</button><button onClick={()=>setUsers(x=>x.filter(y=>y.id!==u.id))} className="inline-flex items-center gap-1 rounded border border-red-200 px-2 py-1 text-xs text-red-700"><Trash2 className="h-3 w-3"/>Remove</button></div></td></tr>)}</tbody></table></div><div className="mt-4 flex items-center gap-2 text-xs text-slate-500"><ShieldCheck className="h-4 w-4"/>Production passwords and permissions belong in the backend.</div></SettingsSection>}