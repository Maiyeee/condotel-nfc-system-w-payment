import PlaceholderPage from '@/components/ui/PlaceholderPage'

export default function SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      phase="Phase 6 — Notifications, Reports & Settings"
      description="Property info, user management, payment provider config, and notification preferences."
    />
  )
}
