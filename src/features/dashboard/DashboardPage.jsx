import PlaceholderPage from '@/components/ui/PlaceholderPage'

export default function DashboardPage() {
  return (
    <PlaceholderPage
      title="Dashboard"
      phase="Phase 3 — Dashboard"
      description="Stat cards, occupancy chart, and recent reservations will be built here once the Guests/Rooms/Reservations data layer (Phase 2) exists."
    />
  )
}
