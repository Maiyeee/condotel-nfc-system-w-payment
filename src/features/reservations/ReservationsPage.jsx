import PlaceholderPage from '@/components/ui/PlaceholderPage'

export default function ReservationsPage() {
  return (
    <PlaceholderPage
      title="Reservations"
      phase="Phase 2 — Core data modules"
      description="Filterable reservations table with status flow: Pending → Confirmed → Checked-in → Checked-out."
    />
  )
}
